[tox]
envlist = clean,py38,py39,report
skipsdist=True

[testenv]
deps = -rrequirements_test.txt
commands = python -m pytest --cov=custom_components.smartbox --cov-append --cov-report=term --cov-report=xml --flake8 --mypy
depends =
    {py38,py39}: clean
    report: py38,py39

[testenv:report]
skip_install = true
commands =
    coverage report
    coverage html

[testenv:clean]
skip_install = true
commands = coverage erase
